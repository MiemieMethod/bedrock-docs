{
    int "priority" : opt
    array "control_flags" : opt
    {
        string "<any array element>" : opt
    }
    float_range "cooldown_range" : opt
    float_range "duration_range" : opt
     "on_start" : opt
     "on_end" : opt
}

