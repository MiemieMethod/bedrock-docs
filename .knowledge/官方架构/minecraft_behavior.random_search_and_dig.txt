{
    int "priority" : opt
    array "control_flags" : opt
    {
        string "<any array element>" : opt
    }
    float_range "cooldown_range" : opt
    float "speed_multiplier" : opt
    int "find_valid_position_retries" : opt
    float "target_dig_position_offset" : opt
    int "search_range_xz" : opt
    int "search_range_y" : opt
    float "goal_radius" : opt
    array "target_blocks" : opt
    {
         "<any array element>" : opt
    }
    float_range "digging_duration_range" : opt
    string "item_table" : opt
    float "spawn_item_after_seconds" : opt
    float "spawn_item_pos_offset" : opt
     "on_searching_start" : opt
     "on_fail_during_searching" : opt
     "on_digging_start" : opt
     "on_item_found" : opt
     "on_fail_during_digging" : opt
     "on_success" : opt
}

