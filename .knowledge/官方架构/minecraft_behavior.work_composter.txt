{
    int "priority" : opt
    array "control_flags" : opt
    {
        string "<any array element>" : opt
    }
    int "sound_delay_min" : opt
    int "sound_delay_min" : opt
    int "sound_delay_max" : opt
    int "sound_delay_max" : opt
    int "goal_cooldown" : opt
    int "active_time" : opt
    bool "can_work_in_rain" : opt
    int "work_in_rain_tolerance" : opt
     "on_arrival" : opt
    float "speed_multiplier" : opt
    int "use_block_min" : opt
    int "use_block_max" : opt
    int "items_per_use_max" : opt
    int "block_interaction_max" : opt
    bool "can_fill_composter" : opt
    bool "can_empty_composter" : opt
    int "min_item_count" : opt
}

